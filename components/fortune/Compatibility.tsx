"use client";

import { CompatibilityExperience } from "@/components/fortune/CompatibilityExperience";

/** English "Compatibility" tool for two names + relationship type. */
export function Compatibility() {
  return <CompatibilityExperience locale="en" />;
}

Compatibility.displayName = "Compatibility";
