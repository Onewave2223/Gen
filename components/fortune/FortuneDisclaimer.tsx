export function FortuneDisclaimer() {
  return (
    <p className="text-xs leading-relaxed text-[var(--muted)]">
      Results are for entertainment purposes only. This is not professional
      advice and does not predict or guarantee future events — treat answers
      as a prompt for reflection, not a guide to action.
    </p>
  );
}

FortuneDisclaimer.displayName = "FortuneDisclaimer";
